<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Bristol Boys — End to End 2012</title>

<style>
:root{
  --bg:#050b1a;
  --card:#101a33;
  --accent:#5bc0be;
  --text:#f1f5f9;
  --muted:#94a3b8;
}

*{box-sizing:border-box}
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto;
  background:linear-gradient(180deg,#020617,#020617,#050b1a);
  color:var(--text);
}

/* HEADER */
header{
  padding:20px;
  text-align:center;
  font-size:20px;
  font-weight:700;
  background:rgba(0,0,0,.4);
  backdrop-filter:blur(10px);
}

/* SECTIONS */
section{
  display:none;
  padding:16px;
  padding-bottom:90px;
}
section.active{display:block}

.card{
  background:linear-gradient(180deg,#0f172a,#020617);
  border-radius:20px;
  padding:18px;
  margin-bottom:18px;
  box-shadow:0 15px 40px rgba(0,0,0,.6);
}

.hero{
  background:url("photos/day1-start.jpg") center/cover;
  height:220px;
  border-radius:22px;
  margin-bottom:18px;
}

h2{margin:0 0 10px 0}
p{color:var(--muted);line-height:1.5}

/* TIMELINE */
.timeline-day{
  cursor:pointer;
}
.timeline-content{
  display:none;
  margin-top:14px;
}
.timeline-day.open .timeline-content{
  display:block;
}

/* STORIES */
.story-card img{
  width:100%;
  border-radius:14px;
  margin-top:12px;
}

/* GALLERY */
.gallery{
  display:flex;
  gap:16px;
  overflow-x:auto;
  scroll-snap-type:x mandatory;
  padding-bottom:10px;
}
.gallery img{
  height:240px;
  border-radius:18px;
  scroll-snap-align:center;
  cursor:pointer;
}

/* LIGHTBOX */
#lightbox{
  position:fixed;
  inset:0;
  background:rgba(0,0,0,.92);
  display:none;
  align-items:center;
  justify-content:center;
  z-index:999;
}
#lightbox img{
  max-width:95%;
  max-height:80%;
  border-radius:18px;
}
#lightbox p{
  position:absolute;
  bottom:24px;
  color:#fff;
  text-align:center;
  width:100%;
  padding:0 20px;
}

/* NAV */
nav{
  position:fixed;
  bottom:0;
  left:0;
  right:0;
  background:#020617;
  display:flex;
  border-top:1px solid #0f172a;
}
nav button{
  flex:1;
  padding:14px 0;
  background:none;
  border:none;
  color:var(--muted);
  font-size:12px;
}
nav button.active{
  color:var(--accent);
  font-weight:600;
}
</style>
</head>

<body>

<header>Bristol Boys — End to End 2012</header>

<!-- HOME -->
<section id="home" class="active">
  <div class="hero"></div>
  <div class="card">
    <h2>The Ride</h2>
    <p>
      John o’ Groats to Land’s End.  
      Over a thousand miles.  
      Weather, fatigue, laughs, breakdowns — and memories that stuck.
    </p>
  </div>
</section>

<!-- TIMELINE -->
<section id="timeline">
  <div class="card timeline-day" onclick="toggleDay(this)">
    <h2>Day 1 — John o’ Groats</h2>
    <p>Tap to expand</p>
    <div class="timeline-content">
      <img src="photos/day1-start.jpg" width="100%" style="border-radius:14px">
      <p>Cold air. Nervous laughs. Photos at the sign before the scale hit us.</p>
    </div>
  </div>

  <div class="card timeline-day" onclick="toggleDay(this)">
    <h2>Day 2 — Highlands</h2>
    <p>Tap to expand</p>
    <div class="timeline-content">
      <img src="photos/map-route.jpg" width="100%" style="border-radius:14px">
      <p>Wind, silence, long miles. Proper riding began here.</p>
    </div>
  </div>
</section>

<!-- STORIES -->
<section id="stories">
  <div class="card story-card">
    <h2>“I didn’t think we’d finish”</h2>
    <p>
      Day three nearly broke us. Nobody quit — but nobody pretended it was easy.
    </p>
    <img src="photos/group-shot.jpg">
  </div>

  <div class="card story-card">
    <h2>“The laughs saved it”</h2>
    <p>
      Every brutal mile had a joke waiting at the next stop.
    </p>
  </div>
</section>

<!-- PHOTOS -->
<section id="photos">
  <h2>Photo Gallery</h2>
  <div class="gallery">
    <img src="photos/day1-start.jpg" onclick="openImage(this,'John o’ Groats start')">
    <img src="photos/map-route.jpg" onclick="openImage(this,'Route map')">
    <img src="photos/group-shot.jpg" onclick="openImage(this,'The team')">
  </div>
  <p style="color:var(--muted)">Swipe • Tap to enlarge</p>
</section>

<!-- LIGHTBOX -->
<div id="lightbox" onclick="closeImage()">
  <img id="lightboxImg">
  <p id="lightboxText"></p>
</div>

<!-- NAV -->
<nav>
  <button class="active" onclick="show('home',this)">Home</button>
  <button onclick="show('timeline',this)">Timeline</button>
  <button onclick="show('stories',this)">Stories</button>
  <button onclick="show('photos',this)">Photos</button>
</nav>

<script>
function show(id,btn){
  document.querySelectorAll("section").forEach(s=>s.classList.remove("active"));
  document.getElementById(id).classList.add("active");
  document.querySelectorAll("nav button").forEach(b=>b.classList.remove("active"));
  btn.classList.add("active");
}

function toggleDay(el){
  el.classList.toggle("open");
}

function openImage(img,caption){
  document.getElementById("lightboxImg").src=img.src;
  document.getElementById("lightboxText").innerText=caption;
  document.getElementById("lightbox").style.display="flex";
}

function closeImage(){
  document.getElementById("lightbox").style.display="none";
}
</script>

</body>
</html>
